# Example usage of the truewatch_default_region data source

# Get all default regions
data "truewatch_default_region" "all" {}

# Output the list of regions
output "regions" {
  value = data.truewatch_default_region.all.regions
}

# Output specific region information
output "region_count" {
  value = length(data.truewatch_default_region.all.regions)
}

# Output the first region's details
output "first_region" {
  value = data.truewatch_default_region.all.regions[0]
  sensitive = false
}
